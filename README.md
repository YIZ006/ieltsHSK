# ieltsHSK

## Build

This repository currently contains documentation only.

Use the production build command below to generate a distributable snapshot:

```bash
make build
```

Build artifacts are written to:

`dist/repository-snapshot.tar.gz`